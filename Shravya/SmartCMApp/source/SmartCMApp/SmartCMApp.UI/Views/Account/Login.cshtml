@model SmartCMApp.UI.ViewModel.LoginViewModel


@{
    ViewBag.Title = "Login";
    Layout = null;
}
<link href="~/Content/bootstrap.min.css" rel="stylesheet" />
<link href="~/Content/Site.css" rel="stylesheet" />

<div class="login-bg">
    <div class="container-fluid min-vh-100 d-flex align-items-center">

        <div class="row w-100">

            <!-- Empty Left Space -->
            <div class="col-lg-6"></div>

            <!-- Login Card -->
            <div class="col-12 col-sm-10 col-md-6 col-lg-4">

                <div class="card shadow-lg ms-lg-4">
                    <h2 class="text-center p-4 login-title">LogIn</h2>

                    @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                    {
                        @Html.AntiForgeryToken()

                        <div class="px-4 py-4">

                            <div class="mb-4">
                                @Html.LabelFor(m => m.UserName, new { @class = "form-label" })
                                @Html.TextBoxFor(m => m.UserName,
                                    new
                                         {
                                        @class = "form-control",
                                        placeholder = "email@example.com",
                                        type = "email"
                                    })
                            </div>

                            <div class="mb-4">
                                @Html.LabelFor(m => m.Password, new { @class = "form-label" })
                                @Html.PasswordFor(m => m.Password,
                                    new
                                         {
                                        @class = "form-control",
                                        placeholder = "Password"
                                    })
                            </div>

                            <div class="text-center mb-3">
                                <button type="submit" class="btn btn-primary px-4">
                                    Log in
                                </button>
                            </div>

                        </div>
                    }

                    <div class="text-start px-4 pb-3">
                        @Html.ActionLink("New User? Register", "Register", "Account",
                            null, new { @class = "text-info" })
                    </div>

                </div>

            </div>

        </div>
    </div>
</div>