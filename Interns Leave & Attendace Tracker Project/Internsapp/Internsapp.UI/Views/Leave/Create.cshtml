@model Internsapp.UI.Models.LeaveModel

@{
    ViewBag.Title = "Apply Leave";
}

<div class="container mt-4">
    <div class="card page-card">

        <!-- Header -->
        <div class="card-header header-create">
            <h5 class="page-title mb-0">Apply Leave</h5>
        </div>

        <!-- Body -->
        <div class="card-body">
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()

                <div class="mb-3">
                    @Html.LabelFor(m => m.InternId, "Intern")
                    @Html.DropDownListFor(
                        m => m.InternId,
                        (SelectList)ViewBag.Interns,
                        "Select Intern",
                        new { @class = "form-select" }
                    )
                    @Html.ValidationMessageFor(m => m.InternId, "", new { @class = "text-danger" })
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        @Html.LabelFor(m => m.FromDate)
                        @Html.TextBoxFor(
                            m => m.FromDate,
                            "{0:yyyy-MM-dd}",
                            new { @class = "form-control", type = "date" }
                        )
                        @Html.ValidationMessageFor(m => m.FromDate, "", new { @class = "text-danger" })
                    </div>

                    <div class="col-md-6 mb-3">
                        @Html.LabelFor(m => m.ToDate)
                        @Html.TextBoxFor(
                            m => m.ToDate,
                            "{0:yyyy-MM-dd}",
                            new { @class = "form-control", type = "date" }
                        )
                        @Html.ValidationMessageFor(m => m.ToDate, "", new { @class = "text-danger" })
                    </div>
                </div>

                <div class="mb-3">
                    @Html.LabelFor(m => m.Reason)
                    @Html.TextAreaFor(
                        m => m.Reason,
                        new { @class = "form-control", rows = 3 }
                    )
                    @Html.ValidationMessageFor(m => m.Reason, "", new { @class = "text-danger" })
                </div>

                <!-- Actions -->
                <div class="d-flex justify-content-between mt-4">
                    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-back" })
                    <input type="submit" value="Apply Leave" class="btn btn-success" />
                </div>
            }
        </div>
    </div>
</div>