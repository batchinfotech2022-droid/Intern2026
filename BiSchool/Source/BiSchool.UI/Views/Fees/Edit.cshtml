@model BiSchool.UI.Models.FeesModel

<div class="container mt-4">
    <div class="card custom-card center-card">
        <div class="card-header bg-primary text-white">
            Edit Fees
        </div>

        <div class="card-body">

            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.Id)

                <div class="mb-3">
                    @Html.LabelFor(m => m.StudentId, new { @class = "form-label fw-semibold" })
                    @Html.TextBoxFor(m => m.StudentId,
                        new { @class = "form-control", @readonly = "readonly" })
                </div>

                <div class="mb-3">
                    @Html.LabelFor(m => m.StudentName, new { @class = "form-label fw-semibold" })
                    @Html.TextBoxFor(m => m.StudentName,
                        new { @class = "form-control", @readonly = "readonly" })
                </div>

                <div class="mb-3">
                    @Html.LabelFor(m => m.Amount, new { @class = "form-label fw-semibold" })
                    @Html.TextBoxFor(m => m.Amount, new { @class = "form-control" })
                </div>

                <div class="mb-3">
                    @Html.LabelFor(m => m.Date, new { @class = "form-label fw-semibold" })
                    @Html.TextBoxFor(m => m.Date, "{0:yyyy-MM-dd}",
                        new { @type = "date", @class = "form-control" })
                </div>

                <div class="d-flex justify-content-between">
                    <button type="submit" class="btn btn-primary">Update</button>
                    @Html.ActionLink("Back", "Index", null,
                        new { @class = "btn btn-secondary" })
                </div>
            }

        </div>
    </div>
</div>